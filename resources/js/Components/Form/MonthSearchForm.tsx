import { Card, CardBody } from "@chakra-ui/react";
import { FC, memo } from "react";

const MonthSearchForm: FC = memo(() => {
    return (
        <Card h="full">
            <CardBody>
                
            </CardBody>
        </Card>
    )
});

export default MonthSearchForm;