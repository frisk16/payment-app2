export type Method = {
    id: number;
    image: string;
    name: string;
};

export type MethodData = {
    image: string;
    name: string;
}

export type MethodError = {
    image: string;
    name: string;
}